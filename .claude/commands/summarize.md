Summarize the work we just completed in this session and save it to `docs/phases/`.

Steps:
1. Read `docs/phases/_template.md` to get the current template structure.
2. Determine an appropriate filename: `docs/phases/YYYY-MM-DD_<short-kebab-slug>.md` using today's date and a 2-4 word slug describing the work (e.g. `2026-05-06_codama-pda-seeds.md`).
3. If a file with that name already exists, append `-2`, `-3`, etc.
4. Fill in the template based on what actually happened in this session:
   - `goal`: what I asked you to do
   - `what was done`: concrete file/command changes — be specific, list filenames
   - `decisions & tradeoffs`: any judgment calls you made or I made
   - `gotchas`: anything that broke or surprised us
   - `open questions` and `next steps`: what's unresolved
5. If a section genuinely has nothing in it, write `_None._` rather than fabricating content.
6. Set `status` honestly: `complete` only if the work actually shipped/landed; otherwise `in-progress` or `blocked`.
7. After writing, show me the file path and a 2-3 line preview.

Do not invent ticket IDs, PRs, or related links. Leave `related: []` empty unless they came up explicitly in our conversation.